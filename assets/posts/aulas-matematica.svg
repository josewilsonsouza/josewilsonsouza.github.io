<svg width="1200" height="630" xmlns="http://www.w3.org/2000/svg">
  <!-- Gradiente verde/amarelo -->
  <defs>
    <linearGradient id="grad-aulas" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#065f46;stop-opacity:1" />
      <stop offset="50%" style="stop-color:#10b981;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#fbbf24;stop-opacity:1" />
    </linearGradient>
    <filter id="glow4">
      <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Fundo -->
  <rect width="1200" height="630" fill="url(#grad-aulas)"/>

  <!-- Lousa simulada -->
  <rect x="100" y="100" width="1000" height="430" fill="rgba(0,0,0,0.15)" rx="10"/>

  <!-- Elementos decorativos -->
  <circle cx="950" cy="150" r="40" fill="rgba(255,255,255,0.1)"/>
  <circle cx="250" cy="480" r="35" fill="rgba(255,255,255,0.1)"/>

  <!-- Equações na lousa -->
  <text x="200" y="200" font-family="Georgia, serif" font-size="32" font-style="italic" fill="rgba(255,255,255,0.6)">
    ax² + bx + c = 0
  </text>
  <text x="700" y="200" font-family="Georgia, serif" font-size="32" font-style="italic" fill="rgba(255,255,255,0.6)">
    ∫ f(x)dx
  </text>
  <text x="200" y="480" font-family="Georgia, serif" font-size="32" font-style="italic" fill="rgba(255,255,255,0.6)">
    lim f(x) = L
  </text>
  <text x="750" y="480" font-family="Georgia, serif" font-size="32" font-style="italic" fill="rgba(255,255,255,0.6)">
    Σ aₙ
  </text>

  <!-- Título principal -->
  <text x="600" y="290" font-family="Arial, Helvetica, sans-serif" font-size="68" font-weight="bold" fill="white" text-anchor="middle" filter="url(#glow4)">
    Aulas de Matemática
  </text>

  <!-- Subtítulo -->
  <text x="600" y="360" font-family="Arial, Helvetica, sans-serif" font-size="36" fill="rgba(255,255,255,0.95)" text-anchor="middle">
    Ensino Médio • Graduação • Reforço Escolar
  </text>

  <!-- Emojis -->
  <text x="600" y="430" font-size="60" text-anchor="middle">🧑‍🏫 📚 📝</text>

  <!-- Rodapé -->
  <text x="600" y="580" font-family="Arial, sans-serif" font-size="24" fill="rgba(255,255,255,0.9)" text-anchor="middle">
    José Wilson C. Souza • (21) 98307-2973
  </text>
</svg>
