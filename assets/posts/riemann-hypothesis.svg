<svg width="1200" height="630" xmlns="http://www.w3.org/2000/svg">
  <!-- Gradiente de fundo roxo/azul -->
  <defs>
    <linearGradient id="grad-riemann" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#4c1d95;stop-opacity:1" />
      <stop offset="50%" style="stop-color:#6366f1;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#8b5cf6;stop-opacity:1" />
    </linearGradient>
    <filter id="glow">
      <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Fundo -->
  <rect width="1200" height="630" fill="url(#grad-riemann)"/>

  <!-- Elementos decorativos -->
  <circle cx="150" cy="150" r="100" fill="rgba(255,255,255,0.05)"/>
  <circle cx="1050" cy="480" r="120" fill="rgba(255,255,255,0.05)"/>
  <circle cx="950" cy="100" r="70" fill="rgba(255,255,255,0.08)"/>
  <circle cx="300" cy="500" r="60" fill="rgba(255,255,255,0.06)"/>

  <!-- Linha crítica decorativa -->
  <line x1="100" y1="315" x2="1100" y2="315" stroke="rgba(255,255,255,0.2)" stroke-width="2" stroke-dasharray="10,5"/>

  <!-- Título principal -->
  <text x="600" y="200" font-family="Arial, Helvetica, sans-serif" font-size="68" font-weight="bold" fill="white" text-anchor="middle" filter="url(#glow)">
    A Hipótese de Riemann
  </text>

  <!-- Subtítulo -->
  <text x="600" y="270" font-family="Arial, Helvetica, sans-serif" font-size="32" fill="rgba(255,255,255,0.9)" text-anchor="middle">
    O grande problema em aberto da Matemática
  </text>

  <!-- Função Zeta -->
  <text x="600" y="380" font-family="Georgia, serif" font-size="56" font-style="italic" fill="rgba(255,255,255,0.95)" text-anchor="middle">
    ζ(s) = 0  ⟹  Re(s) = 1/2 ?
  </text>

  <!-- Emojis decorativos -->
  <text x="600" y="480" font-size="60" text-anchor="middle">∞ 🔢 📐</text>

  <!-- Rodapé -->
  <text x="600" y="560" font-family="Arial, sans-serif" font-size="24" fill="rgba(255,255,255,0.8)" text-anchor="middle">
    José Wilson C. Souza • josewilsonsouza.github.io
  </text>
</svg>
